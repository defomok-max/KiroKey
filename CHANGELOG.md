# Changelog

## Unreleased

- Harden account refresh, routing state persistence, request validation, and
  EventStream parsing.
- Add tests for config, HTTP validation, manifest discovery, request conversion,
  response conversion, EventStream recovery, and UUID validation.
- Add reproducible install/package validation with `package-lock.json`,
  `prepack`, `npm run check`, and CI.
- Add GitHub Actions, Dependabot, EditorConfig, Git attributes, Docker Compose,
  Dockerfile, systemd deployment, and server deployment documentation.
